[pytest]
testpaths = ubc mask ubcsp
python_files = ubc/*.py mask/*.py ubcsp/*.py
addopts = '--tb=short'

filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning

# addopts = '--html=test_report.html' '--self-contained-html'
